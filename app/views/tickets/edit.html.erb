<div class="d-flex align-items-center mt-5 text-center">
    <div class="container w-50">
        <h1 class="display-4">Editing Ticket</h1>

        <%= render 'form', ticket: @ticket %>

        <%= link_to 'Show', @ticket, class: 'btn btn-light' %>
        <%= link_to 'Back', tickets_path, class: 'btn btn-secondary' %>
    </div>
</div>